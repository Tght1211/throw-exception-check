# 不规范异常抛出代码清单

> 生成时间: Fri Aug 01 10:00:18 CST 2025

## 发现 63 个不规范的异常抛出

### ./src/test/java/com/example/service/ExactCaseTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(load.getSource() != OrgSourceType.DING.getValue(),"当前机构已经是钉钉机构无需操作上钉");` |
| 12 | `BizAssert.isTrue(condition, "简单中文消息");` |

### ./src/test/java/com/example/service/ChineseLiteralTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(OrgSourceType.isIncluded(request.getSource()), "非法的机构source");` |
| 12 | `BizAssert.isTrue(orgByTp == null, String.format("该机构已存在,无法更新机构的tpId为%s", request.getTpId()));` |

### ./src/test/java/com/example/service/FaceService.java

| 行号 | 代码片段 |
|------|----------|
| 14 | `BizAssert.notNull(faceRepository, "人脸库尚未初始化,正在执行初始化人脸库...请稍后再试");` |
| 24 | `BizAssert.notNull(faceRepository, "人脸库未初始化");` |
| 27 | `BizAssert.isTrue(false, CommonErrorCode.ILLEGAL_ARGUMENT_EXCEPTION, "人脸与%s相似");` |
| 30 | `ExceptionUtils.throwException("用户%s不存在", "张三");` |

### ./src/test/java/com/example/service/ComplexConditionTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(load.getSource() != OrgSourceType.DING.getValue(), "当前机构已经是钉钉机构无需操作上钉");` |
| 12 | `BizAssert.isTrue(someCondition, "非法的机构source");` |

### ./src/test/java/com/example/service/CompleteTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(load.getSource() != OrgSourceType.DING.getValue(),"当前机构已经是钉钉机构无需操作上钉");` |
| 18 | `BizAssert.isTrue(OrgSourceType.isIncluded(request.getSource()), "非法的机构source");` |
| 23 | `BizAssert.notNull(someObject, "参数不能为空");` |

### ./src/test/java/com/example/service/FinalTestCaseService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(load.getSource() != OrgSourceType.DING.getValue(),"当前机构已经是钉钉机构无需操作上钉");` |
| 12 | `BizAssert.notNull(someObject, "参数不能为空");` |

### ./src/test/java/com/example/service/PureChineseTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(someCondition, "非法的机构source");` |
| 12 | `BizAssert.notNull(someObject, "参数不能为空");` |

### ./src/test/java/com/example/service/EnglishBizAssertTest.java

| 行号 | 代码片段 |
|------|----------|
| 39 | `BizAssert.notNull(orgId, "组织ID不能为空");` |
| 40 | `BizAssert.notNull(email, "邮箱地址无效");` |
| 41 | `BizAssert.isTrue(username != null, "用户名长度不足");` |
| 45 | `throw new RuntimeException("组织ID不能为空");` |

### ./src/test/java/com/example/service/TestIgnorePattern.java

| 行号 | 代码片段 |
|------|----------|
| 20 | `BizAssert.notNull(orgId, "组织ID不能为空");` |
| 21 | `BizAssert.notNull(email, "邮箱地址无效");` |
| 25 | `throw new RuntimeException("组织ID不能为空");` |

### ./src/test/java/com/example/service/UserService.java

| 行号 | 代码片段 |
|------|----------|
| 14 | `throw new RuntimeException("用户名不能为空");` |
| 19 | `throw new IllegalArgumentException("邮箱地址无效");` |
| 26 | `BizAssert.isTrue(username.length() > 3, UserErrorCode.USERNAME_TOO_SHORT, "用户名长度不能少于3个字符");` |
| 48 | `throw new BizException("用户ID不能为空");` |

### ./src/test/java/com/example/service/SpecialCaseTest.java

| 行号 | 代码片段 |
|------|----------|
| 27 | `BizAssert.notNull(orgId, "组织ID不能为空");` |
| 28 | `BizAssert.notNull(email, "邮箱地址格式错误");` |
| 29 | `BizAssert.isTrue(username != null, "用户名验证失败");` |
| 33 | `throw new RuntimeException("组织ID不能为空");` |
| 40 | `BizAssert.isTrue(false, "用户%s不存在", "张三");` |

### ./src/test/java/com/example/service/CompleteIgnoreTest.java

| 行号 | 代码片段 |
|------|----------|
| 53 | `BizAssert.notNull(orgId, "组织ID不能为空");` |
| 54 | `BizAssert.notNull(email, "邮箱地址格式错误");` |
| 55 | `BizAssert.isTrue(username != null, "用户名长度不能少于3位");` |
| 59 | `throw new RuntimeException("组织ID不能为空");` |
| 66 | `BizAssert.isTrue(false, "用户%s不存在", "张三");` |

### ./src/test/java/com/example/service/SimpleTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(orgByTp == null, String.format("该机构已存在,无法更新机构的tpId为%s", request.getTpId()));` |
| 12 | `BizAssert.isTrue(OrgSourceType.isIncluded(request.getSource()), "非法的机构source");` |

### ./src/test/java/com/example/service/TestOptimizationService.java

| 行号 | 代码片段 |
|------|----------|
| 18 | `BizAssert.isTrue(OrgSourceType.isIncluded(request.getSource()), "非法的机构source");` |

### ./src/test/java/com/example/service/SimpleExceptionUtilsTest.java

| 行号 | 代码片段 |
|------|----------|
| 6 | `ExceptionUtils.throwException(OrgErrorCode.AREA_WRONG, "不存在省份数据");` |

### ./src/test/java/com/example/service/StringFormatTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(orgByTp == null, String.format("该机构已存在,无法更新机构的tpId为%s", request.getTpId()));` |

### ./src/test/java/com/example/service/UserCaseTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(load.getSource() != OrgSourceType.DING.getValue(),"当前机构已经是钉钉机构无需操作上钉");` |

### ./src/test/java/com/example/service/AllCasesTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `BizAssert.isTrue(load.getSource() != OrgSourceType.DING.getValue(),"当前机构已经是钉钉机构无需操作上钉");` |
| 14 | `BizAssert.isTrue(OrgSourceType.isIncluded(request.getSource()), "非法的机构source");` |
| 17 | `BizAssert.notNull(someObject, "参数不能为空");` |
| 20 | `ExceptionUtils.throwException(OrgErrorCode.AREA_WRONG, "不存在省份数据");` |
| 23 | `ExceptionUtils.throwException(OrgErrorCode.AREA_WRONG, "不存在省份数据%s");` |

### ./src/test/java/com/example/service/ExceptionUtilsTestService.java

| 行号 | 代码片段 |
|------|----------|
| 7 | `ExceptionUtils.throwException(OrgErrorCode.AREA_WRONG, "不存在省份数据");` |
| 12 | `ExceptionUtils.throwException(OrgErrorCode.AREA_WRONG, "不存在省份数据%s");` |

### ./src/test/java/com/example/service/TestChineseLiteralService.java

| 行号 | 代码片段 |
|------|----------|
| 10 | `BizAssert.notNull(pageNumber, BizErrorCode.PAGE_NUMBER_NULL, "pageNumber必须有值");` |
| 12 | `BizAssert.isTrue(pageNumber > 0, BizErrorCode.PAGE_NUMBER_INVALID, "页码必须大于0");` |
| 20 | `BizAssert.notBlank(name, BizErrorCode.USER_NAME_EMPTY, "用户名不能为空");` |
| 22 | `BizAssert.isTrue(email.contains("@"), BizErrorCode.EMAIL_FORMAT_INVALID, "邮箱格式不正确");` |

### ./src/test/java/com/example/service/RealProjectExample.java

| 行号 | 代码片段 |
|------|----------|
| 29 | `throw new RuntimeException("用户仓库未初始化");` |
| 33 | `BizAssert.notNull(userRepository, "用户服务不可用");` |
| 39 | `BizAssert.isTrue(false, "用户%s已存在", username);` |
| 55 | `throw new BizException("用户ID无效");` |
| 58 | `BizAssert.notNull(userRepository, "用户不存在或已被删除");` |
| 79 | `BizAssert.notNull(orgRepository, "组织服务不可用");` |
| 80 | `throw new RuntimeException("创建部门失败");` |
| 90 | `BizAssert.notNull(userRepository, "批量删除服务未启动");` |

